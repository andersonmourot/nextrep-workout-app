[project]
name = "smellis-backend"
version = "0.1.0"
description = "SMELLIS workout app backend: secure auth + per-user data sync"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "sqlalchemy>=2.0",
    "pydantic[email]>=2.6",
    "pyjwt>=2.8",
]
